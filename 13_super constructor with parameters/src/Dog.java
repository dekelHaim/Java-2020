
public class Dog extends Animal{
	/*
	 Implicit constructor "Dog()" is defined,
	 Because we didn't add a constructor in this class.
	 
	 And in the implicit "Dog()" a "super()" call to the 
	 "Animal()" constructor
	 */
}
